# This file is auto-generated by Firebase.
# https://firebase.google.com/docs/app-hosting/ci-cd

# Re-triggering deployment to resolve pending state.
name: Deploy to Firebase App Hosting on merge to main

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_LIFEAI_ERN1C }}'
          projectId: lifeai-ern1c
          channelId: live
